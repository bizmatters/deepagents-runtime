apiVersion: database.bizmatters.io/v1alpha1
kind: DragonflyInstance
metadata:
  name: deepagents-runtime-cache
  namespace: intelligence-deepagents
  annotations:
    argocd.argoproj.io/sync-wave: "0"  # Create cache BEFORE NATS stream
spec:
  size: micro  # small/micro for preview/dev, medium for production
  storageGB: 5
# Zero-Touch Pattern:
# - Crossplane auto-generates password
# - Connection secret auto-created: deepagents-runtime-cache-conn
